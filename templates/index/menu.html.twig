{% for category in ancestorCategories %}
	<li class="nav-item">
		<a class="nav-link" href="{{ path('category', {'id': category.id, 'slug': category.slug}) }}" style="font-size:2rem">{{ category.name }}</a>
	</li>
{% endfor %}
