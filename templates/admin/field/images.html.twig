
{# <pre>
    {{ dump(ea.entity.instance.images) }}
</pre> #}


{% if ea.entity.instance.images is not null %}
   {# <div class="form-control"> #}
   {% for image in ea.entity.instance.images %}
       {# <a href="#" class="easyadmin-thumbnail" 
       data-featherlight="easyadmin-lightbox-{{ loop.index }}" 
       data-featherlight-close-on-click="anywhere"> #}
       <img src="{{ vich_uploader_asset(image, 'imageFile') | imagine_filter('thumb') }} " class="easyadmin-lightbox" alt="">
       {# </a> #}

       {# <div id="easyadmin-lightbox-{{ loop.index }}" class="easy-admin-lightbox">
            <img src="{{ vich_uploader_asset(image, 'imageFile') | imagine_filter('thumb') }}" alt="">
       </div> #}
   {% endfor %}
   {# </div> #}
{% endif %}