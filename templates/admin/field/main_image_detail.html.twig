{% if ea.entity.instance.mainImage is not null %}
  
       <img src="{{ vich_uploader_asset(ea.entity.instance, 'mainImageFile') | imagine_filter('thumb')}} " class="easyadmin-lightbox" alt="">
       
{% endif %}