
{% if purchase %}
    {% if purchase.purchaseLines | length > 0 %}
        
    {% endif %}
<li class="nav-item">
	<a class="nav-link d-flex flex-row flex-nowrap" href="{{ path('cart_index') }} ">
		<i class="fa fa-shopping-cart" style="color:white"></i>
		<span id="alert-cart" class="badge badge-danger">
        {{purchase.purchaseLines | length}}
        </span>
	</a>
</li>

{% endif %}