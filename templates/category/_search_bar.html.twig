{{ form_start(form) }}
<div class="row mt-4">
	<div class="col-10">
		<div class="col">
			{{ form_row(form.orderby) }}
		</div>
	</div>
	<div id="button" class="col-2 d-flex justify-content-end align-items-end">
		<button type="submit" class="btn btn-success p-2" style="font-size: 2rem">{{ button_label|default('Search') }}</button>
	</div>
</div>

{{ form_end(form) }}
