{{ form_start(form) }}
<div class="mt-4 d-flex flex-row-reverse">
	<div>
		<div>
			{{ form_row(form.orderby) }}
		</div>
	</div>
	<div id="button" class="col-2  align-items-end">
		<button type="submit" class="invisible" style="font-size: 2rem">{{ button_label|default('Search') }}</button>
	</div>
</div>

{{ form_end(form) }}


